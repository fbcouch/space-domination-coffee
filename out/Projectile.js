// Generated by CoffeeScript 1.6.3
(function() {
  var GameObject, Projectile, Ship,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  window.SpaceDom || (window.SpaceDom = {});

  GameObject = window.SpaceDom.GameObject;

  Ship = window.SpaceDom.Ship;

  window.SpaceDom.Projectile = Projectile = (function(_super) {
    __extends(Projectile, _super);

    function Projectile(image, game, specs, ship) {
      this.image = image;
      this.game = game;
      this.ship = ship;
      Projectile.__super__.constructor.call(this, this.image, this.game, specs);
      this.lifetime = specs.lifetime;
    }

    Projectile.prototype.update = function(delta) {
      Projectile.__super__.update.call(this, delta);
      this.lifetime -= delta;
      if (this.lifetime <= 0) {
        return this.isRemove = true;
      }
    };

    Projectile.prototype.canCollide = function(other) {
      return other !== this.ship && !(other instanceof Projectile);
    };

    Projectile.prototype.collide = function(other) {
      if (typeof other.takeDamage === "function") {
        other.takeDamage(this);
      }
      return this.isRemove = true;
    };

    return Projectile;

  })(GameObject);

}).call(this);

/*
//@ sourceMappingURL=Projectile.map
*/
