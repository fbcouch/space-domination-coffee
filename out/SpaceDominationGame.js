// Generated by CoffeeScript 1.6.3
(function() {
  var GameObject, HUD, Particle, Ship, SpaceDominationGame;

  window.SpaceDom || (window.SpaceDom = {});

  GameObject = window.SpaceDom.GameObject;

  Ship = window.SpaceDom.Ship;

  HUD = window.SpaceDom.HUD;

  Particle = window.SpaceDom.Particle;

  window.SpaceDom.SpaceDominationGame = SpaceDominationGame = (function() {
    function SpaceDominationGame(stage, canvas, preload) {
      this.stage = stage;
      this.canvas = canvas;
      this.preload = preload;
      this.setScreen(new SpaceDom.MissionSelectScreen(this.preload, this));
    }

    SpaceDominationGame.prototype.update = function(delta, keys) {
      var _ref;
      this.keys = keys;
      return (_ref = this.screen) != null ? typeof _ref.update === "function" ? _ref.update(delta, this.keys) : void 0 : void 0;
    };

    SpaceDominationGame.prototype.resize = function() {
      return this.screen.resize(this.canvas.width, this.canvas.height);
    };

    SpaceDominationGame.prototype.setScreen = function(screen) {
      var _base;
      if (this.screen != null) {
        if (typeof (_base = this.screen).hide === "function") {
          _base.hide();
        }
        this.stage.removeChild(this.screen);
      }
      this.screen = screen;
      this.screen.resize(this.canvas.width, this.canvas.height);
      this.screen.show();
      return this.stage.addChild(this.screen);
    };

    return SpaceDominationGame;

  })();

}).call(this);

/*
//@ sourceMappingURL=SpaceDominationGame.map
*/
